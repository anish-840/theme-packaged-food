{% comment %}
  Product Info Section — PDP
  Matches screenshot layout:
  Price → Title → Reviews → Short description →
  Allergens → Nutri-score → Variant picker → Buy buttons
{% endcomment %}

{%- liquid
  if section.settings.product != blank
    assign product = section.settings.product
  else
    assign product = closest.product
  endif
  assign allergen_icons = product.metafields.allergens.icons
  assign nutriscore     = product.metafields.product.nutriscore.value | upcase | strip
-%}

<div id="pdp-info-{{ section.id }}" class="pdp-info-section">

  {%- comment %} ── Allergens block ── {%- endcomment %}
  {%- for block in section.blocks -%}
    {%- if block.type == 'allergens' -%}
      {%- if allergen_icons != blank -%}
        <div class="pdp-widget pdp-allergens-wrap" {{ block.shopify_attributes }}>
          <p class="pdp-section-label">{{ block.settings.label | default: 'Allergens' }}</p>
          <div class="pdp-allergens">
            {%- for file in allergen_icons.value -%}
              {%- assign allergen_name = file | split: '/' | last | split: '.' | first | replace: '_', ' ' | replace: '-', ' ' -%}
              <div class="pdp-allergen__item">
                <img
                  src="https://cdn.shopify.com/s/files/1/0965/9793/4353/{{ file }}"
                  alt="{{ allergen_name }}"
                  width="{{ block.settings.icon_size }}"
                  height="{{ block.settings.icon_size }}"
                  loading="lazy"
                >
                <span class="pdp-allergen__name" style="font-size: {{ block.settings.label_size }}px; color: {{ block.settings.label_color }};">
                  {{ allergen_name }}
                </span>
              </div>
            {%- endfor -%}
          </div>
        </div>
      {%- endif -%}

    {%- elsif block.type == 'nutriscore' -%}
      {%- if nutriscore != blank -%}
        {%- if nutriscore == 'A' -%}{%- assign pointer_pct = 10 -%}
        {%- elsif nutriscore == 'B' -%}{%- assign pointer_pct = 30 -%}
        {%- elsif nutriscore == 'C' -%}{%- assign pointer_pct = 50 -%}
        {%- elsif nutriscore == 'D' -%}{%- assign pointer_pct = 70 -%}
        {%- elsif nutriscore == 'E' -%}{%- assign pointer_pct = 90 -%}
        {%- else -%}{%- assign pointer_pct = 50 -%}
        {%- endif -%}

        <div class="pdp-widget pdp-nutriscore-wrap" {{ block.shopify_attributes }}>
          <div class="pdp-nutriscore__header">
            <span class="pdp-section-label pdp-section-label--inline">{{ block.settings.label | default: 'Nutriscore' }}</span>
            <div class="pdp-nutriscore__info-wrap">
              <button class="pdp-nutriscore__info-btn" type="button" aria-label="What is Nutri-score?">i</button>
              <div class="pdp-nutriscore__tooltip">
                {{ block.settings.tooltip_text | default: 'Nutritional Rating System demonstrating the overall nutritional value of food products' }}
              </div>
            </div>
          </div>
          <div class="pdp-nutriscore__bar-wrap">
            <div class="pdp-nutriscore__pointer" style="left: {{ pointer_pct }}%;">
              <svg width="12" height="9" viewBox="0 0 12 9" aria-hidden="true">
                <polygon points="6,9 0,0 12,0" fill="{{ block.settings.pointer_color }}"/>
              </svg>
            </div>
            <div
              class="pdp-nutriscore__bar"
              style="height: {{ block.settings.bar_height }}px; border-radius: {{ block.settings.bar_radius }}px;"
            >
              {%- assign ns_letters = 'A,B,C,D,E' | split: ',' -%}
              {%- assign ns_colors  = '#2d8a2d,#85bb2f,#f5c400,#f07a00,#d93025' | split: ',' -%}
              {%- for letter in ns_letters -%}
                <span
                  class="pdp-nutriscore__zone{% if letter == nutriscore %} is-active{% endif %}"
                  style="background: {{ ns_colors[forloop.index0] }}; font-size: {{ block.settings.label_font_size }}px;"
                >{{ letter }}</span>
              {%- endfor -%}
            </div>
          </div>
        </div>
      {%- endif -%}
    {%- endif -%}
  {%- endfor -%}

  {%- comment %} ── Standard blocks: use content_for on product pages, direct render on other pages ── {%- endcomment %}
  {%- if request.page_type == 'product' and section.settings.product == blank -%}
    {% content_for 'blocks' %}
  {%- else -%}
    {%- for block in section.blocks -%}
      {%- if block.type == 'allergens' or block.type == 'nutriscore' -%}
        {%- comment %} already rendered above {%- endcomment %}
      {%- elsif block.type == 'price' -%}
        <div class="pdp-block-price" {{ block.shopify_attributes }}>
          {%- if product.compare_at_price > product.price -%}
            <div style="display:flex;align-items:baseline;gap:10px;flex-wrap:wrap;">
              <span style="font-size:{{ section.settings.price_size }}px;font-weight:{{ section.settings.price_weight }};color:{{ section.settings.price_color }};">
                {{ product.price | money }}
              </span>
              <span style="font-size:{{ section.settings.compare_price_size }}px;color:{{ section.settings.compare_price_color }};text-decoration:line-through;">
                {{ product.compare_at_price | money }}
              </span>
            </div>
          {%- else -%}
            <span style="font-size:{{ section.settings.price_size }}px;font-weight:{{ section.settings.price_weight }};color:{{ section.settings.price_color }};">
              {{ product.price | money }}
            </span>
          {%- endif -%}
        </div>

      {%- elsif block.type == 'product-title' -%}
        <div {{ block.shopify_attributes }}>
          <h1 style="font-size:{{ section.settings.title_size }}px;font-weight:{{ section.settings.title_weight }};color:{{ section.settings.title_color }};margin:0;line-height:1.2;">
            {{ product.title }}
          </h1>
        </div>

      {%- elsif block.type == 'text' -%}
        {%- if product.description != blank -%}
          <div class="pdp-block-description" {{ block.shopify_attributes }}>
            <div style="font-size:{{ section.settings.desc_size }}px;color:{{ section.settings.desc_color }};line-height:1.6;">
              {{ product.description }}
            </div>
          </div>
        {%- endif -%}

      {%- elsif block.type == 'buy-buttons' -%}
        <div class="pdp-block-cta" {{ block.shopify_attributes }}>
          <a
            href="{{ product.url }}"
            style="
              display:inline-flex;align-items:center;justify-content:center;
              padding:14px 32px;border-radius:40px;
              background:{{ section.settings.cta_bg }};
              color:{{ section.settings.cta_text }};
              font-size:{{ section.settings.cta_font_size }}px;
              font-weight:600;text-decoration:none;
              border:2px solid {{ section.settings.cta_bg }};
              transition:background 0.2s,color 0.2s;
            "
          >{{ section.settings.cta_label | default: 'Shop Now' }}</a>
        </div>

      {%- elsif block.type == 'swatches' -%}
        {%- unless product.has_only_default_variant -%}
          <div class="pdp-block-variants" {{ block.shopify_attributes }}>
            {%- for option in product.options_with_values -%}
              <div style="margin-bottom:12px;">
                <p style="font-size:13px;font-weight:500;margin:0 0 8px;color:{{ section.settings.section_label_color }};">{{ option.name }}</p>
                <div style="display:flex;flex-wrap:wrap;gap:8px;">
                  {%- for value in option.values -%}
                    <span style="
                      padding:6px 14px;border-radius:20px;font-size:13px;cursor:pointer;
                      border:1.5px solid #d0d0d0;background:#fff;color:#1a1a1a;
                    ">{{ value }}</span>
                  {%- endfor -%}
                </div>
              </div>
            {%- endfor -%}
          </div>
        {%- endunless -%}

      {%- endif -%}
    {%- endfor -%}
  {%- endif -%}

</div>

<style>
  #pdp-info-{{ section.id }} {
    display: flex;
    flex-direction: column;
    gap: {{ section.settings.block_gap }}px;
  }

  #pdp-info-{{ section.id }} .pdp-section-label {
    font-size: {{ section.settings.section_label_size }}px;
    font-weight: {{ section.settings.section_label_weight }};
    color: {{ section.settings.section_label_color }};
    margin: 0 0 10px;
    padding: 0;
  }
  #pdp-info-{{ section.id }} .pdp-section-label--inline {
    margin: 0;
    display: inline-block;
  }

  /* ── Widget container ── */
  #pdp-info-{{ section.id }} .pdp-widget {
    padding: {{ section.settings.widget_padding }}px;
    background: {{ section.settings.widget_bg }};
    border: {{ section.settings.widget_border_width }}px solid {{ section.settings.widget_border_color }};
    border-radius: {{ section.settings.widget_radius }}px;
  }

  /* ── Allergens ── */
  #pdp-info-{{ section.id }} .pdp-allergens {
    display: flex;
    flex-wrap: wrap;
    gap: 8px 20px;
    align-items: flex-start;
  }
  #pdp-info-{{ section.id }} .pdp-allergen__item {
    display: flex;
    align-items: center;
    gap: 6px;
  }
  #pdp-info-{{ section.id }} .pdp-allergen__item img {
    display: block;
    object-fit: contain;
    flex-shrink: 0;
  }
  #pdp-info-{{ section.id }} .pdp-allergen__name {
    white-space: nowrap;
    line-height: 1.3;
  }

  /* ── Nutri-score ── */
  #pdp-info-{{ section.id }} .pdp-nutriscore__header {
    display: flex;
    align-items: center;
    gap: 8px;
    margin-bottom: 16px;
  }
  #pdp-info-{{ section.id }} .pdp-nutriscore__info-wrap {
    position: relative;
    display: inline-flex;
  }
  #pdp-info-{{ section.id }} .pdp-nutriscore__info-btn {
    width: 18px; height: 18px;
    border-radius: 50%;
    border: 1.5px solid #aaa;
    background: transparent;
    color: #aaa;
    font-size: 11px;
    font-style: italic;
    font-weight: 600;
    cursor: pointer;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0;
    line-height: 1;
  }
  #pdp-info-{{ section.id }} .pdp-nutriscore__tooltip {
    position: absolute;
    bottom: calc(100% + 8px);
    left: 50%;
    transform: translateX(-50%);
    background: #1a1a1a;
    color: #fff;
    font-size: 12px;
    line-height: 1.5;
    padding: 8px 12px;
    border-radius: 6px;
    width: 240px;
    white-space: normal;
    pointer-events: none;
    opacity: 0;
    transition: opacity 0.15s ease;
    z-index: 20;
    text-align: center;
  }
  #pdp-info-{{ section.id }} .pdp-nutriscore__tooltip::after {
    content: '';
    position: absolute;
    top: 100%;
    left: 50%;
    transform: translateX(-50%);
    border: 5px solid transparent;
    border-top-color: #1a1a1a;
  }
  #pdp-info-{{ section.id }} .pdp-nutriscore__info-wrap:hover .pdp-nutriscore__tooltip {
    opacity: 1;
  }
  #pdp-info-{{ section.id }} .pdp-nutriscore__bar-wrap {
    position: relative;
    padding-top: 20px;
  }
  #pdp-info-{{ section.id }} .pdp-nutriscore__pointer {
    position: absolute;
    top: 0;
    transform: translateX(-50%);
    display: flex;
    justify-content: center;
    line-height: 1;
  }
  #pdp-info-{{ section.id }} .pdp-nutriscore__bar {
    display: flex;
    overflow: hidden;
  }
  #pdp-info-{{ section.id }} .pdp-nutriscore__zone {
    flex: 1;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: 700;
    color: rgba(255,255,255,0.45);
    letter-spacing: 0.02em;
  }
  #pdp-info-{{ section.id }} .pdp-nutriscore__zone.is-active {
    color: #ffffff;
  }
</style>

{% schema %}
{
  "name": "Product info",
  "tag": "div",
  "class": "pdp-info-section",
  "settings": [
    {
      "type": "header",
      "content": "Product"
    },
    {
      "type": "product",
      "id": "product",
      "label": "Product",
      "info": "Leave empty to use the current page product"
    },
    {
      "type": "header",
      "content": "Layout"
    },
    {
      "type": "range",
      "id": "block_gap",
      "label": "Gap between blocks",
      "min": 0, "max": 40, "step": 2, "unit": "px",
      "default": 16
    },
    {
      "type": "header",
      "content": "Section label style"
    },
    {
      "type": "range",
      "id": "section_label_size",
      "label": "Font size",
      "min": 11, "max": 22, "step": 1, "unit": "px",
      "default": 14
    },
    {
      "type": "select",
      "id": "section_label_weight",
      "label": "Font weight",
      "options": [
        { "value": "400", "label": "Regular" },
        { "value": "500", "label": "Medium" },
        { "value": "600", "label": "Semi-bold" },
        { "value": "700", "label": "Bold" }
      ],
      "default": "500"
    },
    {
      "type": "color",
      "id": "section_label_color",
      "label": "Color",
      "default": "#1a1a1a"
    },
    {
      "type": "header",
      "content": "Widget container"
    },
    {
      "type": "color",
      "id": "widget_bg",
      "label": "Background",
      "default": "#f5f5f3"
    },
    {
      "type": "range",
      "id": "widget_padding",
      "label": "Padding",
      "min": 8, "max": 32, "step": 2, "unit": "px",
      "default": 16
    },
    {
      "type": "range",
      "id": "widget_radius",
      "label": "Border radius",
      "min": 0, "max": 24, "step": 2, "unit": "px",
      "default": 10
    },
    {
      "type": "color",
      "id": "widget_border_color",
      "label": "Border color",
      "default": "#e8e8e8"
    },
    {
      "type": "range",
      "id": "widget_border_width",
      "label": "Border width",
      "min": 0, "max": 4, "step": 1, "unit": "px",
      "default": 1
    },
    {
      "type": "header",
      "content": "Price (homepage)"
    },
    {
      "type": "range",
      "id": "price_size",
      "label": "Price font size",
      "min": 14, "max": 48, "step": 1, "unit": "px",
      "default": 28
    },
    {
      "type": "select",
      "id": "price_weight",
      "label": "Price font weight",
      "options": [
        { "value": "400", "label": "Regular" },
        { "value": "500", "label": "Medium" },
        { "value": "600", "label": "Semi-bold" },
        { "value": "700", "label": "Bold" }
      ],
      "default": "700"
    },
    {
      "type": "color",
      "id": "price_color",
      "label": "Price color",
      "default": "#1a1a1a"
    },
    {
      "type": "range",
      "id": "compare_price_size",
      "label": "Compare-at price font size",
      "min": 12, "max": 32, "step": 1, "unit": "px",
      "default": 18
    },
    {
      "type": "color",
      "id": "compare_price_color",
      "label": "Compare-at price color",
      "default": "#888880"
    },
    {
      "type": "header",
      "content": "Title (homepage)"
    },
    {
      "type": "range",
      "id": "title_size",
      "label": "Font size",
      "min": 16, "max": 72, "step": 2, "unit": "px",
      "default": 32
    },
    {
      "type": "select",
      "id": "title_weight",
      "label": "Font weight",
      "options": [
        { "value": "400", "label": "Regular" },
        { "value": "500", "label": "Medium" },
        { "value": "600", "label": "Semi-bold" },
        { "value": "700", "label": "Bold" }
      ],
      "default": "700"
    },
    {
      "type": "color",
      "id": "title_color",
      "label": "Color",
      "default": "#1a1a1a"
    },
    {
      "type": "header",
      "content": "Description (homepage)"
    },
    {
      "type": "range",
      "id": "desc_size",
      "label": "Font size",
      "min": 12, "max": 20, "step": 1, "unit": "px",
      "default": 15
    },
    {
      "type": "color",
      "id": "desc_color",
      "label": "Color",
      "default": "#3d3d3a"
    },
    {
      "type": "header",
      "content": "CTA button (homepage)"
    },
    {
      "type": "text",
      "id": "cta_label",
      "label": "Button label",
      "default": "Shop Now"
    },
    {
      "type": "color",
      "id": "cta_bg",
      "label": "Background",
      "default": "#1a2e0a"
    },
    {
      "type": "color",
      "id": "cta_text",
      "label": "Text color",
      "default": "#ffffff"
    },
    {
      "type": "range",
      "id": "cta_font_size",
      "label": "Font size",
      "min": 12, "max": 20, "step": 1, "unit": "px",
      "default": 15
    }
  ],
  "blocks": [
    {
      "type": "text",
      "name": "Text"
    },
    {
      "type": "image",
      "name": "Image"
    },
    {
      "type": "price",
      "name": "Price"
    },
    {
      "type": "review",
      "name": "Reviews"
    },
    {
      "type": "sku",
      "name": "SKU"
    },
    {
      "type": "swatches",
      "name": "Swatches"
    },
    {
      "type": "product-title",
      "name": "Product title"
    },
    {
      "type": "buy-buttons",
      "name": "Buy buttons"
    },
    {
      "type": "custom-liquid",
      "name": "Custom Liquid"
    },
    {
      "type": "@app"
    },
    {
      "type": "allergens",
      "name": "Allergens",
      "settings": [
        {
          "type": "text",
          "id": "label",
          "label": "Section label",
          "default": "Allergens"
        },
        {
          "type": "range",
          "id": "icon_size",
          "label": "Icon size",
          "min": 16, "max": 48, "step": 2, "unit": "px",
          "default": 28
        },
        {
          "type": "range",
          "id": "label_size",
          "label": "Label font size",
          "min": 10, "max": 18, "step": 1, "unit": "px",
          "default": 13
        },
        {
          "type": "color",
          "id": "label_color",
          "label": "Label color",
          "default": "#3d3d3a"
        }
      ]
    },
    {
      "type": "nutriscore",
      "name": "Nutri-score",
      "settings": [
        {
          "type": "text",
          "id": "label",
          "label": "Section label",
          "default": "Nutriscore"
        },
        {
          "type": "text",
          "id": "tooltip_text",
          "label": "Tooltip text",
          "default": "Nutritional Rating System demonstrating the overall nutritional value of food products"
        },
        {
          "type": "range",
          "id": "bar_height",
          "label": "Bar height",
          "min": 20, "max": 56, "step": 2, "unit": "px",
          "default": 32
        },
        {
          "type": "range",
          "id": "bar_radius",
          "label": "Bar border radius",
          "min": 0, "max": 20, "step": 2, "unit": "px",
          "default": 6
        },
        {
          "type": "range",
          "id": "label_font_size",
          "label": "Zone label size",
          "min": 10, "max": 20, "step": 1, "unit": "px",
          "default": 13
        },
        {
          "type": "color",
          "id": "pointer_color",
          "label": "Pointer color",
          "default": "#1a1a1a"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Product info",
      "blocks": [
        { "type": "price" },
        { "type": "product-title" },
        { "type": "review" },
        { "type": "text" },
        { "type": "allergens" },
        { "type": "nutriscore" },
        { "type": "swatches" },
        { "type": "buy-buttons" }
      ]
    }
  ]
}
{% endschema %}
